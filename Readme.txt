ParallelTestwithScriptinPY.py - This is the grinder script. This is the main file which executes also the script mentioned in an array in teh file parallel. makes sure the filenames in array doesnt not have .py extension.

ParallelTestwithScriptfromPropertiesFile.py - This is the grinder script. The filenames of the scripts to be executed are retrived from the properties file which triggers this file. 
.Makes sure the filenames in array doesnt not have .py extension.

ParllelTest.properties=Properties file to trigger the grinder script.

ParalletTest.py - This requires Grinder 3.11. The file with teh *.py extension are read from the mentioned directory and excuted in parallel.